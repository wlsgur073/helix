{
  "name": "helix",
  "version": "0.1.0",
  "description": "Helix: verifiable, trust-indexed cross-session memory (provenance firewall, fail-closed) with optional Codex dual-verify, for Claude Code via MCP. Voice/persona is yours to customize (CLAUDE.md or an output style).",
  "author": { "name": "wlsgur073" },
  "keywords": ["assistant", "judgment", "memory", "dual-verify"],
  "mcpServers": {
    "helix": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/bin/helix-mcp.mjs"]
    }
  }
}
