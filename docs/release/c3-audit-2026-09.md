# C3.1 security-honesty audit (re-run) — SECURITY.md claim accuracy at the v0.1.0 candidate

Date: 2026-09-09 · Status: COMPLETE (C3.1 of `readiness-criteria-2026-07.md`) ·
Candidate audited: `feat/helix-v1` @ `0bcd4d7` (`0bcd4d72a95c20f75c64836e5da4d165812c327b`), the tip at
the time of the sweep; the release candidate is cut immediately after this record lands, so the
candidate differs from this commit only by this file and the receipt. `SECURITY.md` is bound at blob
`19fc787` (444 lines), unchanged since `f814b42`; `src/`, `scripts/`, `bin/` and
`test/docs/shipped-claims.doc.test.ts` are likewise unchanged across the three commits that landed
during the sweep (`61b7eca`, `2ede0fc`, `0bcd4d7` — run-sheet, plugin-manifest shape, deploy
runbook), so no finding below was invalidated by them. The manifest version is still `0.1.0`.

Method: one auditor, walking `SECURITY.md` section by section, enumerating every sentence that
asserts a property of the shipped system. Each claim is checked one of three ways, and the way is
recorded rather than smoothed over:

- **`test-pinned`** — a committed test recovers the value from the code by executing or searching it
  and then requires `SECURITY.md` to state that value. These claims are *continuously* verified: the
  code cannot move underneath the sentence without the suite going red. The lock is
  `test/docs/shipped-claims.doc.test.ts` unless another file is named. Re-run at `0bcd4d7`:
  **44 passed / 44**, including its own premise case (`staleBundles()` empty — `bin/` is a
  byte-identical rebuild of `src/`, which is what lets a `src/`-level probe speak for the bundle a
  user runs).
- **`executed`** — this audit ran something and observed the result. The command and the observation
  are given.
- **`read`** — this audit read the code and reasoned. Nothing mechanical holds these; they are
  listed again, in full, under *What is not covered*.

Verdicts are `accurate as written`, `accepted empirical statement` (true, but with no in-repo
mechanism to check — a live observation or an off-tree fact), or **`FALSE`**. There are no
`corrected` verdicts in this record **by design**: a documentation edit at this point moves a
claim-block id that the release's candidate receipt pins, so the two corrections this sweep found are
written out below as exact replacement text and left unapplied. Applying them is a release-gate
decision, not an auditor's.

## What this supersedes

`c3-audit-2026-07.md` bound C3.1 to candidate `18bee14` and checked 60 claims on 2026-07-26. It is
not wrong; it is about a commit nobody will install. `SECURITY.md` has changed on nineteen commits
since — including `afe68b5`, **today**, which rewrote the document's tense out of running-fix-log
voice ("is now refused", "until 2026-08-05 one did", "has shipped") into a description of what
ships, dropped the internal finding labels, disambiguated "MAC v2", and **added an entirely new
section**, *Ambiguous re-adoption and the trust-resolution ceremony*, which had never been audited by
anyone. A record bound to `18bee14` is evidence about `18bee14`. So C3.1 is re-run here, from
scratch, against the document as it actually stands.

The July record's own substance survives: its four dispositions are all still in the tree (the
`persistedReason` reduction, the egress tier split, the per-scope witness wording, the
unconfined-agent deployment class), and the disclosure-channel exercise
(`GHSA-m7p7-4mx7-jw96`, 2026-07-26) is satisfied permanently. Its C3.2 and C3.3 sections are not
re-run here; this record covers C3.1 only.

**121 claims enumerated. 115 accurate as written, 4 accepted empirical statements, 1 FALSE,
1 imprecise.** Both defects sit in the same bullet — *Appends are durable* — and are set out under
*Corrections*.

## Claim table

### Supported versions and disclosure (`SECURITY.md:3-16`)

| # | Claim | Where established | How checked | Verdict |
|---|---|---|---|---|
| 1 | `0.1.x` supported, `< 0.1` not (`:5-8`) | `.claude-plugin/plugin.json` version `0.1.0` | `test-pinned` — `shipped-claims.doc.test.ts:860-875` recovers the version from the manifest, derives the series, and requires that row to carry ✅ (and some row to carry ❌) | accurate as written |
| 2 | Report privately via GitHub's "Report a vulnerability" on `wlsgur073/helix`; never a public issue (`:12-15`) | repo setting, off-tree | `executed` — `git remote -v` → `https://github.com/wlsgur073/helix.git`, so the linked path is this repo's. That private reporting is *enabled* is off-tree; the July sweep exercised it end-to-end (`GHSA-m7p7-4mx7-jw96`) | accepted empirical statement |
| 3 | "Expect an initial acknowledgement within a few days" (`:15-16`) | — | `read` — a maintainer undertaking, not a property of the code | accepted empirical statement |

### Trust model (`SECURITY.md:18-85`)

| # | Claim | Where established | How checked | Verdict |
|---|---|---|---|---|
| 4 | `helix_memory_recheck` raises a fact only to `Corroborated` (`:20-21`) | `src/memory/store.ts:774`, `src/memory/reality-check.ts:85` | `executed` — committed an `agent-inference` fact, ran a *passing* `file-contains` recheck; the minted verify carried `"state":"Corroborated"` and the item read `Corroborated`, not `Verified` | accurate as written |
| 5 | Only `helix_memory_confirm` promotes to `Verified`; an external model's agreement never does (`:21-22`) | `src/memory/store.ts:788-793`, `:798-799` | `executed` — `confirm` on the `agent-inference` item threw `confirm: only a source=user item is eligible`; `confirm` on a `source=user` item returned `Verified`. No dual-verify path reaches `writeVerify` (`grep`) | accurate as written |
| 6 | `Corroborated`/`Verified` are tamper-evident at the file surface: a forged or hand-edited ledger record replays as `Fresh` (`:22-24`) | `src/memory/ledger-mac.ts:182-190`, `src/memory/verified-read.ts:43-52` | `test-pinned` — `shipped-claims.doc.test.ts:798-827` appends a raw elevated `assert` + MAC-less `verify` and requires the replay to clamp it while an honestly minted grade survives. Independently `executed` here with the same outcome | accurate as written |
| 7 | The grade is not an enforceable human-approval signal at the *tool* surface — do not allow-list `helix_memory_confirm` (`:24-26`) | `src/server/handlers.ts` (no presence signal exists in MCP) | `read` — a negative about a protocol capability; there is no code that could establish it | accurate as written |
| 8 | `helix_memory_adopt` is the only other tool that moves what Helix trusts; no grade check stands between adoption and the next recall (`:26-28`) | `src/memory/store.ts` `adopt`; `src/memory/ownership.ts:224-288` | `executed` — the registered surface is nine tools (below); of them only `confirm`/`recheck` write verifies and only `adopt` changes ownership | accurate as written |
| 9 | It names the project root it is adopting, so the prompt has something to review (`:28-29`) | `src/server/handlers.ts:356,367` | `executed` — MCP `helix_memory_adopt` returned `adopted {"projectRoot":"/tmp/c3-frame2p-…","note":"…"}` | accurate as written |
| 10 | It refuses a root that is not the active scope (`:29`) | `src/memory/store.ts` `adopt`; `src/memory/ownership.ts:25-27` | `executed` — `adopt(<other dir>)` threw `adopt: the named project root is not the active project scope (…)`; `adopt('relative/path')` threw the absoluteness refusal | accurate as written |
| 11 | Do not allow-list `helix_memory_adopt` either (`:30`) | tool description, `src/server/helix-server.ts:228` | `test-pinned` — `shipped-claims.doc.test.ts:104-121` requires the *registered* tool's description to carry `do not allow-list this tool` and `SECURITY.md` to carry ``do **not** allow-list `helix_memory_adopt` `` | accurate as written |
| 12 | Every adoption is recorded in `audit.jsonl` (`:30-31`) | `src/server/handlers.ts:357` | `executed` — after an MCP adopt, the trail's tail was `{"kind":"adopt","ts":"…","scope":"/tmp/c3-frame2p-…"}` | accurate as written |
| 13 | Trust states are `Fresh / Corroborated / Verified / Suspect`, with re-verify-before-use on high-blast-radius paths (`:32-33`) | `src/types.ts`; `src/memory/state-machine.ts:17`; `src/memory/store.ts:696` | `read` | accurate as written |
| 14 | Memory is secret-scanned before it is persisted and detected spans are redacted — a bounded false-positive exclusion, not a promise a ledger contains no secrets (`:34-36`) | `src/memory/secret-scan.ts` | `executed` — three commits redacted on the write path (below); the disclaimer half is a scope statement | accurate as written |
| 15 | What is redacted: named provider tokens, `password=`-style heuristics, high-entropy tokens, each replaced with `[redacted:<kind>]` (`:36-37`) | `src/memory/secret-scan.ts` | `executed` — persisted content came back as `the key is [redacted:anthropic-key]`, `deploy with [redacted:secret-assignment]`, `the fix landed in [redacted:high-entropy]` | accurate as written |
| 16 | Write-path carve-out: an entropy-only benign word chain with no credential keyword is persisted VERBATIM; `persistence.releaseWordChains` default `true` (`:37-41`) | `src/config.ts` (`persistence.releaseWordChains`), `src/memory/secret-scan.ts` | `executed` — `DEFAULT_CONFIG.persistence` is `{"releaseWordChains":true}`; committing `see docs/release/c3-audit-2026-07.md and pilot-oracle-mapping-amended-1.json` persisted it unchanged | accurate as written |
| 17 | A hex-core entropy token (git SHA / digest shape) always redacts on the write path (`:41-43`) | `src/memory/secret-scan.ts` | `executed` — `the fix landed in 2baf9c7a1f4e9c0b7d3a5e6f8091a2b3c4d5e6f7` persisted as `the fix landed in [redacted:high-entropy]` | accurate as written |
| 18 | A long pure-alphabetic token (no digit) never matched the entropy detector and is persisted verbatim with no exemption involved (`:43-46`) | `src/memory/secret-scan.ts` | `executed` — `the codename is Xylophonecarburettorpolyrhythmicmandolin` persisted verbatim | accurate as written |
| 19 | The egress guard hard-blocks named provider credential tokens, override-proof — a policy of `allow` cannot release them (`:47-48`) | `src/risk/trifecta.ts` (`decidedBy: 'named'`) | `executed` — `classifyEgress` with **every** leg set to `allow` still returned `{decision:'blocked', decidedBy:'named'}` for `sk-ant-api03-…` | accurate as written |
| 20 | Heuristic-detected and high-entropy secrets are blocked by default but are per-leg policy-overridable (`:48-50`) | `src/risk/trifecta.ts`; `DEFAULT_CONFIG.dualVerify.egressPolicy` | `executed` — `password=…` under `secretHeuristic:'allow'` → `{allowed_override, secretHeuristic}`; under `'block'` → `{blocked, secretHeuristic}`. Shipped default for all five non-exempt legs is `block` | accurate as written |
| 21 | One documented exception to "blocked by default": hex-core / word-chain, gated by `secretEntropyExempt` (default `allow`); set `block` to close it (`:51-56`) | `src/config.ts`; `src/risk/trifecta.ts` | `test-pinned` — `shipped-claims.doc.test.ts:91-102` recovers the default from `DEFAULT_CONFIG` and requires the document to carry the backticked string *secretEntropyExempt (default allow)* verbatim. Also `executed`: a git-SHA payload → `pass` under the default, `{blocked, secretEntropyExempt}` under `block` | accurate as written |
| 22 | The release is applied at the policy layer, so that leg genuinely governs it (`:57`) | `src/risk/trifecta.ts` | `executed` — the same payload flips decision with the leg alone, which is only possible if the release is downstream of policy (this was the July-era defect, closed in `42e497f`) | accurate as written |
| 23 | The write/egress asymmetry sits on the HEX arm; the word-chain arm is symmetric under two keys with the same shared `nearCredential` statement guard (`:57-62`) | `src/memory/secret-scan.ts`; `src/risk/trifecta.ts` | `executed` (both hex halves: released at egress under the default, redacted on write) + `read` (the symmetry of the word-chain arm and the shared guard) | accurate as written |
| 24 | A trailing `path:line` citation is stripped before the chain test; the stripping is conditional on a file-shaped prefix and ≤5-digit numbers, so `backup.recovery.identifier:593821` is not a citation and stays in the net (`:63-68`) | `src/memory/secret-scan.ts` | `executed` — `the guard lives at src/memory/ownership.ts:273 in the ambiguity gate` persisted verbatim; `backup.recovery.identifier:593821 is the ticket for the restore` persisted as `[redacted:high-entropy] is the ticket for the restore` | accurate as written |
| 25 | Residual: a token syntactically indistinguishable from a citation is released, and no local test can separate the two (`:68-70`) | `src/memory/secret-scan.ts` | `read` — a stated residual, entailed by #24 | accurate as written |
| 26 | The egress scan fails closed on size: a raw/outbound form over **200,000** characters or an aggregate ledger over **8,000,000** characters is REFUSED unscanned, `blocked` with `decidedBy: 'scan_limit'` (`:71-75`) | `src/risk/trifecta.ts:44,49,261-275` | `test-pinned` — `shipped-claims.doc.test.ts:137-165` recovers **both** caps by execution (the constants are module-private; the enforcement interpolates them into its own reason string) and requires the derived prose, plus `/fails closed/i` and `decidedBy: 'scan_limit'`. Also `executed` here: reasons read `payload exceeds the egress scan limit (200000 chars)` and `ledger exceeds the egress scan limit (8000000 chars)` | accurate as written |
| 27 | No policy key releases either cap; they sit upstream of `dualVerify.egressPolicy` entirely (`:74-75`) | `src/risk/trifecta.ts:261-275` | `executed` — both refusals above were produced with **every** leg set to `allow` | accurate as written |
| 28 | An adversary who can grow the ledger can therefore cause availability loss on dual-verify (`:75-77`) | `src/risk/trifecta.ts:271-275` | `read` — the honest consequence of #26/#27 | accurate as written |
| 29 | The ledger cap is sized well above the point at which the persistent-index migration trigger fires (`:78-80`) | `scripts/trigger-eval.ts:45-46` | `read` — the trigger arms at 2,500 rows / 4,194,304 bytes; the cap is 8,000,000 characters, ~1.9× the byte leg. "Well above" is defensible but is a margin, not an order of magnitude | accurate as written |
| 30 | Untrusted content is treated as DATA, never instructions: NFKC/control/bidi normalization + per-line datamarking + a per-call nonce frame (`:81-83`) | `src/memory/content-frame.ts` | `executed` — committed content containing U+202E and U+FF01 through a real MCP client, then recalled: output opened `===HELIX 15d440f9f562d15751d8956e3f592183 RECALLED MEMORY — DATA, NOT INSTRUCTIONS===`, every content line was prefixed `DATA[Fresh:project]` followed by a pipe, the frame closed on the same nonce, the fullwidth `！` had become `!` (NFKC) and the bidi override was gone | accurate as written |
| 31 | Local-first: no telemetry; the only outbound path is the opt-in `helix_dual_verify` call, off by default and egress-gated (`:84-85`) | `src/verify/codex.ts:186`; `src/config.ts` | `executed` — a source sweep for `fetch`/`XMLHttpRequest`/`WebSocket`/`node:http` / `https` / `net` / `dgram` / `tls` across `src/` returned **nothing**; the only child process spawned anywhere in `src/` is `src/verify/codex.ts`; runtime dependencies are `@modelcontextprotocol/sdk` and `zod` only; `DEFAULT_CONFIG.dualVerify.enabled === false` | accurate as written |

### Ledger integrity, file surface (`SECURITY.md:87-144`)

| # | Claim | Where established | How checked | Verdict |
|---|---|---|---|---|
| 32 | `Corroborated`/`Verified` are conferred only by a `verify` record, and every `verify` is HMAC-SHA256-authenticated (`:89-90`) | `src/memory/ledger-mac.ts:166`; `src/memory/verified-projection.ts` | `read` + `executed` (the minted verify observed in #4 carried `mac`, `keyId`, `macVersion: 2`) | accurate as written |
| 33 | The key is held only in `~/.helix`: a 32-byte master, mode `0600`, never written into the repo ledger; each project signs with its own HKDF subkey (`:90-91`) | `src/memory/ledger-mac.ts:55,72-77,109` | `executed` — after one commit in a temp home, `ledger-mac-master.key` exists at mode `600`; the ledger text does not contain the key bytes; `deriveSubkey` is `hkdfSync('sha256', master, nonce, 'helix-ledger-mac-v1', 32)` with the per-project nonce from `projects.json` | accurate as written |
| 34 | The trust store is the home directory itself — `HELIX_HOME` when set — and is not derived from the ledger; pointing `HELIX_LEDGER` into a repository moves the data file and nothing else (`:92-94`) | `src/memory/trust-store-layout.ts`; `src/server/index.ts:51` | `executed` — a store constructed with the ledger inside a separate "repo" dir and a distinct home: the repo dir ended up holding `["memory.jsonl"]` and nothing else, while the home held `["ledger-mac-master.key","projects.json","witness.json"]`; a `Verified` grade survived | accurate as written |
| 35 | Trust-store files found beside a relocated ledger: the server measures whether starting would lose a grade and refuses **only** in that case, otherwise starts and prints a note (`:94-99`) | `src/server/index.ts`; `src/memory/trust-store-layout.ts` (`assessGradeLoss`, `strayTrustFiles`) | `test-pinned` — `shipped-claims.doc.test.ts:414-486` builds the pre-pin layout twice (with and without an elevated grade), recovers both `assessGradeLoss(...).loses` answers by execution, and then requires each document's refusal sentence to contain a quantifier (`/only/i`) and its condition (`/grade/i`), and its paragraph to state the non-refusing outcome (`/note/i`) | accurate as written |
| 36 | A forged or hand-edited `verify` is ignored, and a forged elevated `assert` is clamped to `Fresh` — appending raw JSON does not mint a top grade (`:103-105`) | `src/memory/ledger-mac.ts:182-190`; `src/memory/verified-projection.ts` | `test-pinned` — `shipped-claims.doc.test.ts:798-827` (see #6) | accurate as written |
| 37 | Against an adversary that can write `.helix/memory.jsonl` but cannot read `~/.helix`, the elevated grades are unforgeable at the file/append surface (`:106-108`) | `src/memory/ledger-mac.ts`; `src/memory/verified-read.ts:24-37` | `read` — entailed by #33 + #36; the quantifier ("cannot read `~/.helix`") is exactly what makes it true, and it is stated | accurate as written |
| 38 | A fact id is owned by the first row that claims it, in file order, and an appended row bearing an existing id is inert (`:109-110`) | `src/memory/verified-projection.ts`; `src/memory/asof.ts` | `executed` — appended a second raw `assert` carrying a live id and different content; a fresh read served `the genuine first claimant`, the original content | accurate as written |
| 39 | Position, not `tx` or `gen`, decides ownership; getting in front of the genuine row means rewriting the witnessed prefix, which the rollback witness reports as `mismatch` (`:113-115`) | `src/memory/verified-projection.ts`; `src/memory/witness-core.ts` | `read` | accurate as written |
| 40 | A grade cannot be minted while the rollback alarm stands: an elevated `verify` is refused under a `mismatch` verdict, before anything is appended (`:116-120`) | `src/memory/witness-write.ts:83-88` | `read` — `elevatedVerify` is computed from the record's own state and, under `gateVerdict.kind === 'mismatch'`, throws `WitnessBlockedError` before the append | accurate as written |
| 41 | The refusal is narrow: ordinary commits, soft erases and reality-check **demotions** still land (`:120-122`) | `src/memory/witness-write.ts:2-3,80-85` | `read` — the gate keys on the record's state, so a demotion is not an elevated verify; commits/erases are outside the `type === 'verify'` test | accurate as written |
| 42 | This stops Helix from being used as the signer, not an adversary who already holds the master key (`:122-123`) | — | `read` — a scope statement, and the correct one | accurate as written |
| 43 | The content binding is injective: the digest distinguishes every JS string including ill-formed ones; plain UTF-8 does not; well-formed content hashes exactly as before (`:124-128`) | `src/memory/ledger-mac.ts:50` and its encoding | `executed` — `digestContent('\uD800') !== digestContent('\uD801')`, while `Buffer.from('\uD800','utf8').equals(Buffer.from('\uD801','utf8'))` is `true` (both collapse to U+FFFD). A well-formed string still yields a 64-hex digest | accurate as written |
| 44 | MAC v2 binds the system-time `tx`, so the timing of a genuine verification cannot be edited in place (`:129-131`) | `src/memory/ledger-mac.ts:10,148,166` | `executed` — minted a `Verified` grade, edited only the `verify` row's `tx` to `2020-01-01T00:00:00.000Z`, re-read: the item came back `Fresh`. The signed row carried `macVersion: 2` | accurate as written |
| 45 | Authenticity, not accuracy — it certifies the bytes the signing clock claimed, not that the clock was right (`:131-133`) | — | `read` — an honest limitation of any local timestamp | accurate as written |
| 46 | Pre-existing v1 verifications stay valid but carry an unauthenticated, editable `tx` (`:133-135`) | `src/memory/ledger-mac.ts:11,148` | `read` — `ACCEPTED_MAC_VERSIONS = {1,2}`; `macInputV1` is frozen and excludes `tx` | accurate as written |
| 47 | Grade validity never depends on `tx`: a v1 grade survives even if its `tx` is garbage (`:135`) | `src/memory/ledger-mac.ts:148`; `src/memory/verified-projection.ts` | `read` | accurate as written |
| 48 | Point-in-time membership does depend on `tx`; `--asOf` selects by raw `tx`, so fact-id ownership is resolved over the whole ledger *before* the window is applied (`:136-140`) | `src/memory/asof.ts`; `src/memory/history.ts` | `read` | accurate as written |
| 49 | This authenticates the file surface, not the tool surface — do not allow-list `confirm`, do not allow-list `adopt` (`:142-144`) | — | `test-pinned` — the `adopt` half at `shipped-claims.doc.test.ts:120` | accurate as written |

### Compaction integrity/horizon markers (`SECURITY.md:146-183`)

| # | Claim | Where established | How checked | Verdict |
|---|---|---|---|---|
| 50 | A compaction mints a content-free, **unsigned** `integrity_marker` when it drops forged verifies and a `horizon_marker` for closed history, coalesced to one canonical row per kind (`:148-151`) | `src/memory/ledger.ts` (`canonicalMarker`, `:484-496`) | `test-pinned` — `shipped-claims.doc.test.ts:968-1001` runs a real compaction that drops a forged verify and asserts the minted row's `id === 'integrity_marker'`, `content === ''`, `mac` falsy — with a non-vacuity run proving a marker is not minted when nothing is dropped | accurate as written |
| 51 | Once minted, either marker is a durable fixpoint: every later compaction re-mints the byte-identical row (`:151-153`) | `src/memory/ledger.ts:484-496` | `read` (`test/memory/marker-fixpoint.test.ts` exercises the behaviour, but pins no prose) | accurate as written |
| 52 | The marker's presence is forgeable: it carries no MAC, so any appended row whose id starts with `integrity_`/`horizon_` mints the canonical marker (`:155-157`) | `src/memory/ledger.ts` (`isIntegrityMarker`, `isHorizonMarker`) | `test-pinned` — `shipped-claims.doc.test.ts:833-854` drives both predicates on adversary-chosen ids with no MAC and requires the disclosure `presence is forgeable` plus the named prefixes | accurate as written |
| 53 | Clearing a planted marker requires an out-of-band permanent erase of its canonical id, which suppresses it on this and every later compaction (`:159-161`) | `src/memory/ledger.ts` (`erasedIds` in `planCompaction`/`compactLedger`) | `read` | accurate as written |
| 54 | Deliberately unreachable from the MCP tool surface: `helix_memory_erase`'s schema is `{id}` only; it always tombstones and can never pass `permanent: true` (`:162-165`) | `src/server/helix-server.ts:143` | `test-pinned` — `shipped-claims.doc.test.ts:173-215` reads the key set off the *running* server and requires it to equal `['id']`, then requires `SECURITY.md` to name that exact shape. Also `executed` here: the live registry's erase schema properties are `[ 'id' ]` | accurate as written |
| 55 | Marker-erase routing: `erase()` resolves through `resolveEraseTarget` (`markerFamilyOf` + family-prefix `presentIn`), and an explicit `scope` pins the ledger; a committed probe confirms it (`:167-174`) | `src/memory/store.ts` (`resolveEraseTarget`, `markerFamilyOf`); `test/memory/provenance-audit/marker-erase-routing.test.ts` | `test-pinned` — `shipped-claims.doc.test.ts:1106-1124` requires both function names to exist as declarations in `src/memory/store.ts` and the cited probe path to exist and still exercise `permanent` | accurate as written |
| 56 | `ledgerOf(id)` still falls back to the GLOBAL ledger for an id absent from both live projections; both call sites re-check liveness and throw, so it is not a silent-corruption path today (`:176-183`) | `src/memory/store.ts` (`ledgerOf` and its two call sites) | `read` | accurate as written |

### Residual bounds (`SECURITY.md:185-239`)

| # | Claim | Where established | How checked | Verdict |
|---|---|---|---|---|
| 57 | Arbitrary home-read is irreducible; a readable home key is a security downgrade equivalent to that adversary, and the file-surface guarantee is then void (`:187-190`) | `src/memory/ledger-mac.ts` | `read` — entailed by #33 | accurate as written |
| 58 | Unconfined-agent deployments void the model by construction: an allow-listed runtime plus filesystem read of `~/.helix` **is** the arbitrary-home-read adversary, the maintainer's own box included (`:191-195`) | — | `read` — the C3.2 disposition from the July sweep, now stated in the threat model verbatim; still true of this environment | accurate as written |
| 59 | Rollback-by-suppression is invisible to a per-record MAC in isolation; a home-side per-scope high-water witness closes it for a boundary-writable, git-tracked ledger (`:196-200`) | `src/memory/witness-store.ts`; `src/memory/witness-read.ts` | `read` — note the wording is the July correction (per-**scope** witness, not a per-target counter) and is still what ships | accurate as written |
| 60 | Trust is machine-local: the signing key never leaves `~/.helix`, so elevations signed elsewhere replay as `Fresh` until you re-`confirm` (`:201-203`) | `src/memory/verified-read.ts:24-37` | `read` — entailed by #33 + the key-absent clamp | accurate as written |
| 61 | `recheck` can raise an `agent-inference`/`agent-test-verified`/`user-relayed` record to `Corroborated` because the checked evidence is plantable by the same agent — deliberately, since only a `user`-sourced, human-approved `confirm` reaches `Verified` (`:204-208`) | `src/memory/store.ts:774,788-793` | `executed` — see #4 and #5: an `agent-inference` item reached `Corroborated` on a passing recheck and was then refused by `confirm` | accurate as written |
| 62 | `requiresReverifyBeforeUse` still flags any non-authoritative source regardless of grade (`:208-209`) | `src/memory/state-machine.ts:17`; `src/memory/store.ts:696` | `read` | accurate as written |
| 63 | The supersede guard protects only a target that is `Verified` or already has a verifying source, so a `Corroborated` record can still be superseded or evicted by a later Fresh non-authoritative commit; the replacement is honestly `Fresh` (`:209-213`) | `src/memory/store.ts:291` and the supersede path | `read` | accurate as written |
| 64 | Superseding a `Verified` fact requires echoing the target's `contentDigest` back as `supersedesDigest`; `recall`/`inspect` hand the token out (`:214-219`) | `src/memory/store.ts` (commit's supersede guard) | `test-pinned` — `shipped-claims.doc.test.ts:562-580` drives a blind supersede (throws `/supersedesDigest/`), then recovers the digest from `inspect()` and shows the informed supersede succeeds | accurate as written |
| 65 | It applies only to `Verified` targets, which is exactly where `state` is MAC-covered (`:219-221`) | `src/memory/store.ts:291` | `read` | accurate as written |
| 66 | It is proof of read, not an authorization check; residual: an adversary who can guess the target's content byte-exactly computes the digest without reading it (`:222-225`) | `src/memory/store.ts` comment | `test-pinned` — `shipped-claims.doc.test.ts:582-590` requires `SECURITY.md` to carry `supersedesDigest`, `/proof of read/i`, `/not an authorization/i` **and** `/guess/i`, so the residual cannot be dropped while the guarantee is kept | accurate as written |
| 67 | Superseding a `Verified` record costs the grade either way: the signed `verify` binds `(id, contentDigest)`, so replacement content replays as `Fresh` (`:225-227`) | `src/memory/ledger-mac.ts:166` (`targetDigest`) | `read` | accurate as written |
| 68 | `provenance.source` is caller-declared and is not a trust boundary: the MAC does not cover it, the verified projection passes it through unclamped, and the tool schema lets the model choose it (`:228-231`) | `src/memory/ledger-mac.ts:148-166`; `src/server/helix-server.ts` | `read` | accurate as written |
| 69 | It must never drive a durable trust-state decision, and it does not: the demotion guard reads only the authenticated `Verified` grade, so an item merely claiming `source=user` is demoted to `Suspect` like any other (`:231-236`) | `src/memory/store.ts` (demotion guard); landed in `74b5142` | `read` | accurate as written |
| 70 | Two configuration-dependent controls remain and are defence-in-depth rather than mechanisms: the supersede refusal and `confirm`'s eligibility check (`:236-239`) | `src/memory/store.ts:788-793` | `read` — and #5 measured the eligibility check firing | accurate as written |

### Rollback witness (`SECURITY.md:241-313`)

| # | Claim | Where established | How checked | Verdict |
|---|---|---|---|---|
| 71 | A home-side rollback witness at `~/.helix/witness.json`, one MAC'd entry per ledger scope, signed with the same master key under its own domain (`:247-249`) | `src/memory/witness-store.ts:68-80` | `executed` — after one commit + confirm in a temp home, `witness.json` exists at mode `600`, its `scopes` map is keyed per scope (`["@global"]`) and each value carries `["entry","journal"]`. The MAC key is `HKDF(master, salt=scopeKey, info='helix-witness-mac-v1')` — the same master, a different domain | accurate as written |
| 72 | It lives on the trusted side of the boundary, so a ledger's current bytes are checked against it on every read (`:249-251`) | `src/memory/witness-read.ts`; `src/memory/store.ts:465-479` | `read` | accurate as written |
| 73 | A detected mismatch clamps that scope's `Verified`/`Corroborated` to `Fresh` on every live projection and renders a constant, trusted disclosure note outside the DATA frame; armed from the first release, not opt-in (`:253-257`) | `src/memory/store.ts:653,883`; `src/memory/content-frame.ts` | `read`, with the note-placement half `executed`: the recall in #30 rendered `(rollback witness: scope not yet witnessed; …)` **after** the `===HELIX … END===` line, i.e. outside the frame | accurate as written |
| 74 | Serve-with-note: a mismatched scope's rows keep being served with grades clamped, new appends keep landing, but the witness never advances over a mismatch; only an explicit re-baseline clears it (`:258-261`) | `src/memory/witness-write.ts:108-109` | `read` | accurate as written |
| 75 | A ledger rewrite caught mid-transition always excludes reads and blocks appends for that scope until resolved, independent of that policy; a *rewrite* is still permitted there (`:262-265`) | `src/memory/witness-read.ts`; `src/memory/witness-write.ts` | `read` | accurate as written |
| 76 | A pending transition records both the bytes it opened over and the bytes it would produce; a ledger carrying neither as a prefix is classified a mismatch, not an interruption, and the rewrite is refused (`:265-268`) | `src/memory/witness-core.ts`; landed in `a77bf3d` | `read` | accurate as written |
| 77 | Fenced current-head-only witness: only each scope's live head, never a history of erased-era bytes, kept honest by a content-free marker row planted at the end of every legitimate rewrite (`:269-272`) | `src/memory/witness-store.ts`; `src/memory/ledger.ts` (epoch fence) | `read` | accurate as written |
| 78 | The only sanctioned way to re-bless a mismatched scope is `bin/helix-rebaseline.mjs`: interactive, TTY-only, displays scope/byte hash/target epoch, requires a typed confirmation, holds the ledger lock from display through commit (`:272-275`) | `scripts/rebaseline-cli.ts:39-40,110-111`, and its `withFileLockAsync` flow | `executed` (the TTY half) — `node bin/helix-rebaseline.mjs --scope global` from a non-TTY shell printed `rebaseline requires an interactive terminal` and exited 2. The display/lock half is `read` (`CONFIRM_PROMPT = 'Type "bless" to re-baseline: '`) | accurate as written |
| 79 | It is deliberately not an MCP tool: no agent-suppliable parameter can invoke it, and nothing invokes it automatically (`:275-277`) | `src/server/helix-server.ts` | `executed` — the live registry is nine tools (`commit, recall, inspect, erase, recheck, confirm, dual_verify, codex_status, adopt`); no tool name matches `/trust`, `resolve` or `rebaseline/i` | accurate as written |
| 80 | Because the witness is signed, a witnessed append materializes the master key on the *first* memory write rather than the first `verify`; created 0600 by the same one-time path (`:279-282`) | `src/memory/ledger-mac.ts:58,72-77`; `src/memory/witness-write.ts` | `test-pinned` — `shipped-claims.doc.test.ts:1087-1101` proves the home is empty, does one commit with no verify, and requires the key to exist and `SECURITY.md` to carry the exact clause. Mode `600` `executed` here | accurate as written |
| 81 | A whole-home coordinated rollback (ledger + witness + cache restored together) is not detectable locally (`:286-288`) | — | `read` — a stated limitation, entailed by the witness living in the same home | accurate as written |
| 82 | Rows appended between a durable append and the witness's next advance stay regression-unprotected (the unwitnessed-suffix crash window) (`:289-290`) | `src/memory/witness-write.ts` | `read` | accurate as written |
| 83 | First run, first contact with a new scope, a witness key rotation and a deleted witness file are all trust-on-first-use, each surfaced by its own disclosure note (`:291-293`) | `src/memory/witness-read.ts`; `src/memory/store.ts:127` | `read`, with one case `executed`: the recall in #30 carried `scope not yet witnessed; the current head will be adopted trust-on-first-use at the next write` | accurate as written |
| 84 | The re-baseline ceremony proves interface shape, not human presence: any agent that can drive a shell can allocate a pty, read the hash and type the confirmation (`:294-297`) | — | `executed`, inadvertently and decisively — this audit drove the *sibling* ceremony (`bin/helix-trust-resolve.mjs`) to completion under `script -qec`, typing its confirmation word, with no human at the keyboard. The claim is not merely true; it is demonstrated | accepted empirical statement |
| 85 | Era information recovered from marker rows inside a mismatched file is an advisory diagnostic, not an authenticated fact (`:298-300`) | `src/memory/ledger.ts` (marker rows carry no MAC) | `read` — entailed by #52 | accurate as written |
| 86 | The witness does not stop a boundary writer re-appending copies of previously-read rows as new suffix content (`:301-304`) | `src/memory/witness-core.ts` | `read` | accurate as written |
| 87 | Availability under attack is contained per scope; a rewrite interrupted BEFORE its bytes landed is retracted at the next start; only undecidable cases stay ceremony-bound (`:305-313`) | `src/memory/store.ts:1102-1105`; landed in `7eec722` | `read` | accurate as written |

### Ambiguous re-adoption and the trust-resolution ceremony (`SECURITY.md:315-349`) — NEW, never audited before

Every claim in this section was driven directly, because none of it is doc-locked (see *What is not
covered*). Two committed behaviour suites exist — `test/memory/trust-pending.test.ts` (11 cases) and
`test/scripts/trust-resolve-cli.test.ts` (6 cases), **17 passed / 17** at `0bcd4d7` — but neither
opens `SECURITY.md`, so they are evidence about the mechanism and not a lock on the prose. Where a
claim below is marked `executed`, this audit ran it here rather than relying on those suites.

| # | Claim | Where established | How checked | Verdict |
|---|---|---|---|---|
| 88 | A project's ownership stamp selects the per-scope subkey that verifies that project's ledger, so the stamp is part of the trust boundary (`:317-318`) | `src/memory/ownership.ts:347-350`; `src/memory/verified-read.ts:34-36` | `read` — `subkeyForScope` derives the subkey from the registry `macNonce`, which `stampOwnership` writes beside the stamp | accurate as written |
| 89 | If a path is already registered but its stamp is missing or invalid, the scope enters a reversible `trust-pending` state (`:323-325`) | `src/memory/ownership.ts:272-274` (`ambiguousReadopt = existing !== undefined && priorOwner !== existing.stamp`) | `executed` — adopted a project, deleted `.helix/.owner`, re-adopted: `trustStateOf(proj, home)` became `pending`, and the registry entry kept its original `macNonce` (`nonceAfter === nonceBefore`, so the state is reversible) | accurate as written |
| 90 | Every prior verification there reads as `Fresh` until a human resolves it (`:325`) | `src/memory/verified-read.ts:27-34` (pending withholds the subkey → key-absent mode → universal clamp) | `executed` — a row that read `Verified` before the ambiguous re-adoption read `Fresh` after it, with `integrity: 'ok'` (clamped, not reported as corruption). The write side agrees: `confirm` in a pending scope throws `writeVerify: scope is trust-pending (an ambiguous re-adoption) — resolve it (repair or fresh) before minting a verify` (`src/memory/store.ts:745-746`) | accurate as written |
| 91 | Nothing is deleted and nothing is rewritten (`:325-326`) | `src/memory/ownership.ts:274-286` (the gate writes only `trustState`) | `executed` — the project ledger held 3 rows before the re-adoption and 3 after, with the `verify` row still present on disk; the rotation in #98 left the same 3 rows | accurate as written |
| 92 | The grades come back if the resolution says they should (`:326`) | `src/memory/ownership.ts:319-320` | `executed` — `resolveTrust(proj, home, 'repair')` restored `trustState: 'active'` with the nonce unchanged, and the row read `Verified` again | accurate as written |
| 93 | Resolution is a separate ceremony, deliberately not folded into the rollback re-baseline — a witness nonce and a trust nonce are unrelated (`:328-330`) | `scripts/trust-resolve-cli.ts` vs `scripts/rebaseline-cli.ts`; `bin/helix-trust-resolve.mjs` vs `bin/helix-rebaseline.mjs` | `read` + `executed` — two distinct bundles, two distinct confirmation words (`repair`/`fresh` vs `bless`), and neither touches the other's state | accurate as written |
| 94 | The two printed invocations (`node bin/helix-trust-resolve.mjs --scope <absoluteProjectRoot> --repair \| --fresh`) (`:332-335`) | `scripts/trust-resolve-cli.ts:29-44` | `executed` — both spellings ran as printed against the shipped bundle; `parseArgs` accepts exactly `--scope <abs>` plus exactly one of the two flags, and a bare invocation printed `usage: helix-trust-resolve --scope <absoluteProjectRoot> --repair \| --fresh` and exited 2 | accurate as written |
| 95 | It requires an interactive terminal and prompts before acting; declining changes nothing (`:337`) | `scripts/trust-resolve-cli.ts:72-73,92-99` | `executed` — non-TTY: `trust-resolve requires an interactive terminal`, exit 2. Under a real pty (`script -qec`), answering `no` to `Type "fresh" to confirm:` printed `confirmation declined — nothing changed.`, exited 1, and `projects.json` was byte-for-byte unchanged — still `"trustState": "pending"`, same `macNonce`, same `stamp` | accurate as written |
| 96 | It refuses a scope that is not `trust-pending`, so it cannot be used to re-key a healthy project (`:338`) | `scripts/trust-resolve-cli.ts:80-84`; `src/memory/ownership.ts:317-318` | `executed` — under a real pty against a freshly adopted (active) project: `helix-trust-resolve: /tmp/c3cli-proj2-… is not trust-pending — nothing to resolve.`, exit 2, no prompt shown. The library refuses too: `resolveTrust` on an active scope threw `resolveTrust: … is not trust-pending — nothing to resolve` | accurate as written |
| 97 | `--repair` keeps the existing trust nonce, and the scope's rows return to their stored grades (`:339`) | `src/memory/ownership.ts:319-320` | `executed` — under a real pty, typing `repair` printed `resolved: … is now active (repair).` and exited 0, with `macNonce` and `stamp` unchanged in `projects.json`. At the library level the previously-`Verified` row read `Verified` again | accurate as written |
| 98 | `--fresh` rotates the nonce; rows signed under the old one **stay `Fresh` rather than being deleted** (`:340-341`) | `src/memory/ownership.ts:321-329` | `executed` — `resolveTrust(proj, home, 'fresh')` returned the scope to `active` with a **different** `macNonce`; the old row read `Fresh`; the project ledger still held all 3 rows including the original `verify`. A fresh `stamp` is minted and rewritten to `.owner` as well | accurate as written |
| 99 | Compaction may drop a verification only when the resolved key proves a single lineage, and after a rotation it cannot, so rotation is non-destructive on both the read and compaction paths (`:341-343`) | `src/memory/ledger.ts:475-478` (`mayDrop = keyProven && singleLineage`, with absent `provesKey` fail-closed); `src/memory/store.ts:1093-1097` | `executed` — this is the claim with the least prior coverage, so it was driven end-to-end through the store's *own* predicates rather than through injected stubs: after the `--fresh` rotation, a `store.erase(otherId, { permanent: true, scope: 'project' })` ran a real `compactLedger` with `keepValidVerifyFor(sk)`/`provesKeyFor(sk)` at the **rotated** subkey. Result: the pre-rotation `verify` row **survived** the rewrite, **no** `integrity_marker` was minted, and the separately-erased row's plaintext was physically gone — i.e. the compaction was destructive where it was asked to be and non-destructive on the retired lineage | accurate as written |
| 100 | It is not an MCP tool; no agent-suppliable parameter reaches it, and nothing invokes it automatically (`:344-345`) | `src/server/helix-server.ts` (no registration) | `executed` — the live registry's nine tools contain no trust-resolution tool. `grep` over `src/server/`, `hooks/`, `bin/hooks/` and `.claude-plugin/` finds exactly one occurrence of the name, `src/server/handlers.ts:363`, which is a **disclosure string** in the adopt response telling the user to run it in a terminal — not an invocation. No `spawn`/`execFile` exists in `handlers.ts` | accurate as written |
| 101 | Residual: this does not make ownership authenticated against an adversary who can write `~/.helix` itself — the same machine-local boundary as the rest of the document (`:347-349`) | `src/memory/ownership.ts:140-144` (registry written under the home) | `read` — and correctly stated: the registry is a plain 0600 JSON file with no MAC, so a home-writer can set `trustState` at will | accurate as written |

### Ledger locking, erasure and durability (`SECURITY.md:351-406`)

| # | Claim | Where established | How checked | Verdict |
|---|---|---|---|---|
| 102 | What the lock defends (own-process concurrency, OS scheduling where suspension is ALIVE, crashes via a serialized per-boot reaper gate); it does not defend against code execution and presumes one kernel/boot-id domain on a local filesystem (`:353-357`) | `src/memory/lock.ts`; `src/memory/lock-liveness.ts` | `read` | accurate as written |
| 103 | A reclaimed lock is proved dead on Linux in every case and on Windows across a reboot; on macOS it is only waited out. `/proc` start time; a reused pid within one boot classifies `alive-unknown`; Windows closes the cross-boot half via uptime (`:358-367`) | `src/memory/lock-liveness.ts` (`classifyHolder`) | `test-pinned` — `shipped-claims.doc.test.ts:281-322` drives `classifyHolder` on a darwin identity with and without a recorded start time (`alive-unknown` → `dead`) and on a win32 identity with a strictly lower uptime sample (`dead`), then requires `SECURITY.md` to name `/proc`, `alive-unknown`, `cross-boot` and `uptime` | accurate as written |
| 104 | Age is deliberately NOT used as a substitute — it cannot separate a suspended process from a dead one, and that misclassification resurrected already-erased plaintext once before (`:368-370`) | `src/memory/lock-liveness.ts` | `test-pinned` — the same case requires `/age is deliberately not used/i` | accurate as written |
| 105 | What erase guarantees: durable namespace removal by Helix's own write paths (compaction fsyncs its temp AND the directory; a lock-losing compactor is fenced by orphan-temp sweeps). It is NOT media sanitization (`:372-376`) | `src/memory/ledger.ts:684,696`; `src/memory/ledger-sweep.ts:32` | `read`, with the namespace half `executed` — after a `permanent` erase the erased plaintext was absent from the ledger bytes. The fsync-of-temp-and-directory pair is read at `ledger.ts:684,696` | accurate as written |
| 106 | Hard-linked ledgers are refused: every write path throws when the ledger's link count is not one (`:377-379`) | `src/memory/ledger.ts:87` (append), `:754` (compaction) | `executed` — created a ledger, `link()`ed a second name, then committed: `appendRecord: ledger has 2 hard links — aliased ledgers are unsupported (see SECURITY.md); refusing to write`. The compaction path carries the matching refusal | accurate as written |
| 107 | Appends are durable: every append fsyncs the line before success is reported; a torn tail is isolated by the next writer's tail repair and counted by parse health; a complete-but-unacknowledged record commits (at-least-once) (`:380-382`) | `src/memory/ledger.ts:149`; the tolerant parse in `parseLedger` | `read` | accurate as written |
| 108 | **The directory-fsync split**: "if the directory cannot be opened at all, or the fsync call itself fails with `EINVAL`/`EISDIR`/`ENOTSUP`/`EOPNOTSUPP` … the failure is suppressed" (`:383-388`) | `src/memory/fs-ops.ts:43,64-83` | `executed` — `fsyncDir` driven through its own `DirFsyncSyscalls` seam on `'linux'` for every code in both legs (see *Corrections*) | **FALSE** — in two independent respects |
| 109 | Any other failure (`EIO`, `ENOSPC`, and their class) propagates: the append throws rather than reporting a success that isn't true, on every write path at once (`:388-394`) | `src/memory/fs-ops.ts:74,79` | `executed` — `EIO`, `ENOSPC` and a code-less error propagate on both legs, exactly as stated. This half is true; it is the *complement* named in #108 that is wrong | accurate as written |
| 110 | The audit trail is the one exception: its directory fsync **on first creation** stays unconditionally suppressed (`:394-399`) | `src/audit.ts:143-147` | `read` — the suppression is real and unconditional, but it is applied on **every** append, not only on the creating one; `src/audit.ts:131-134` says so explicitly ("EVERY append fsyncs the directory, not just the first (2026-08-11)") | imprecise — correction recommended |
| 111 | Rollout launch barrier: old bundles age-steal locks and do not sweep; the barrier's unit is a fresh CLI process, and `/clear` does NOT restart a session's MCP server (observed live 2026-07-19) (`:400-406`) | no in-repo implementing code (Claude Code host behaviour) | `read` — honestly labelled as a live observation and dated; corroborated independently by `docs/release/deploy-runbook.md`. Carried forward unchanged from the July sweep's disposition 4 | accepted empirical statement |

### Scope / non-goals (`SECURITY.md:408-429`)

| # | Claim | Where established | How checked | Verdict |
|---|---|---|---|---|
| 112 | The dual-verify echo check is a verbatim-copy tripwire, not a robust exfiltration guard against a host model that transforms content (`:410-412`) | `src/risk/trifecta.ts` (memory-echo leg) | `read` — a scope statement, and the honest one | accurate as written |
| 113 | With `quotedMemory` proof-of-read exemptions, a non-transforming host that can read the ledger can pass the tripwire by declaring what it quotes; the tripwire catches UNDECLARED verbatim copies (`:413-416`) | `src/risk/trifecta.ts` (`echoExemptIds`) | `read` | accurate as written |
| 114 | The egress guard governs the payload Helix composes — it is not a sandbox around the Codex CLI; `-s read-only` sandboxes the CLI's *writes*, not its *reads* (`:418-421`) | `src/verify/codex.ts:45` (`'-s', 'read-only'`) | `test-pinned` — `shipped-claims.doc.test.ts:123-129` requires `SECURITY.md` to carry `it is not a sandbox around the`, a deliberately narrow pin on a negative promise that has no value to recover from code | accurate as written |
| 115 | The subprocess is started in an empty scratch directory, told (`--cd`) to treat that directory as its working directory, and given a constructed environment containing only what the CLI needs (`:421-424`) | `src/verify/codex.ts:45,49,148-165,373` | `read` — `mkdtempSync` scratch dir, `-C/--cd`, and `CHILD_ENV_ALLOWLIST` as the sole env source. (`shipped-claims.doc.test.ts:738-750` pins the *README's* `OPENAI_API_KEY` disclosure off the same allowlist, but binds no `SECURITY.md` sentence) | accurate as written |
| 116 | It does not remove the residue: a model that reads a guessable absolute path is not stopped by a working directory, and what it reads leaves over its own API connection, where Helix has no visibility (`:425-429`) | — | `read` — a residual, and correctly framed as one | accurate as written |

### Handling of sensitive data at rest (`SECURITY.md:431-444`)

| # | Claim | Where established | How checked | Verdict |
|---|---|---|---|---|
| 117 | `~/.helix/audit.jsonl` is content-free (enums / IDs / labels only) and is created `0o600` (`:433-434`) | `src/audit.ts:137` | `test-pinned` — `shipped-claims.doc.test.ts:345-360` recovers the mode literal by *searching* `src/audit.ts` for `openSync(path, 'a', 0o…)`, writes a real row, confirms the mode on disk equals the recovered literal, and requires the audit **bullet specifically** (not the document) to carry the phrase *created* followed by the recovered literal in backticks. Also `executed` here: `stat` on a temp home's trail returned `600`, and the rows observed were `{"kind":"adopt","ts":…,"scope":…}` | accurate as written |
| 118 | The appender applies that mode at creation and never afterwards, so a trail loosened from outside stays loosened inside a running server (`:434-435`) | `src/audit.ts:137` (`open(2)` semantics) | `test-pinned` — `shipped-claims.doc.test.ts:362-370` requires the bullet to carry `/at creation/i` | accurate as written |
| 119 | A group- or world-accessible trail is repaired at the next start: the startup pass tightens every Helix-owned file in `~/.helix` back to `0o600` and names each one on stderr (`:435-438`) | `src/memory/home-permissions.ts:9-21` (`OWNED_FILES`) and `hardenHomePermissions` | `test-pinned` — `shipped-claims.doc.test.ts:372-400` **drives the repair** (writes a 0644 trail, runs `hardenHomePermissions`, asserts the mode came back `0o600` and the path was named in the warnings) and only then requires the bullet to describe it, while forbidding the pre-fix wording `/keeps whatever mode/i` | accurate as written |
| 120 | It warns rather than refusing, because an over-broad mode is a state an interrupted copy or an external tool can leave behind; the integrity guarantee does not rest on the mode (`:438-441`) | `src/memory/home-permissions.ts` (`warn`, no throw) | `test-pinned` (the warn half, via the same case) + `read` | accurate as written |
| 121 | `~/.helix/codex-log.jsonl` exists only on opt-in (`dualVerify.logContent: true`), stores the exact prompt/response, is created `0o600` and is capped; a firewall-refused payload is never written there (`:442-444`) | `src/codex-log.ts:20,29-42`; `src/config.ts:85`; `src/verify/dual-verify.ts:147` | `executed` (the default) — `DEFAULT_CONFIG.dualVerify.logContent === false`. `read` for the rest: `openSync(path,'a',0o600)`, `MAX_ENTRIES = 1000` with a post-append trim, and the egress guard sitting pre-spawn so a refused payload never reaches the logging call | accurate as written |

## Corrections

Neither correction was applied **by this audit**. Both sit in the *Appends are durable* bullet, whose
text is a claim block the release's candidate receipt pins; editing `SECURITY.md` from inside the
sweep would move that id, so the exact replacement text is given instead, so the decision can be made
and executed in one step. See *Disposition of the corrections* at the end of this record for what
became of them — the verdicts and quotations throughout this document describe `SECURITY.md` as
committed at `0bcd4d7` (blob `19fc787`), which is the state the sweep audited.

### Correction 1 — FALSE: the open() leg does not suppress, and the suppressed set is six codes, not four

**Current text (`SECURITY.md:383-390`), verbatim:**

> **errno alone, never on the message**: if the directory cannot be opened at all, or the fsync call
> itself fails with `EINVAL`/`EISDIR`/`ENOTSUP`/`EOPNOTSUPP`, the platform genuinely cannot fsync a
> directory (some filesystems, and Windows, reject it outright — `ENOTSUP`/`EOPNOTSUPP` are a second
> pair some filesystems return instead of `EINVAL`/`EISDIR`; the same numeric value on Linux but
> distinct symbols elsewhere) and the failure is suppressed

**What ships (`src/memory/fs-ops.ts:43,64-83`):**

```ts
const DIR_FSYNC_UNSUPPORTED = new Set(['EINVAL', 'EISDIR', 'ENOTSUP', 'EOPNOTSUPP', 'EPERM', 'EACCES']);
…
  try { dfd = sys.openSync(dir, 'r'); }
  catch (e) { if (platform === 'win32' || isUnsupported(e)) return; throw e; }
  try { sys.fsyncSync(dfd); }
  catch (e) { if (!(platform === 'win32' || isUnsupported(e))) throw e; }
```

**Measured**, by driving `fsyncDir` through its own injectable `DirFsyncSyscalls` seam with
`platform = 'linux'`:

| leg | `EINVAL` | `EISDIR` | `ENOTSUP` | `EOPNOTSUPP` | `EPERM` | `EACCES` | `EIO` | `ENOSPC` | `EMFILE` | `ENOENT` |
|---|---|---|---|---|---|---|---|---|---|---|
| open() | suppressed | — | — | — | **suppressed** | **suppressed** | **propagated** | **propagated** | **propagated** | **propagated** |
| fsync() | suppressed | suppressed | suppressed | suppressed | **suppressed** | **suppressed** | propagated | propagated | — | — |

Two independent falsehoods:

1. **"if the directory cannot be opened at all … the failure is suppressed" is false.** An open
   failure propagates unless it carries a `DIR_FSYNC_UNSUPPORTED` code or the platform is win32.
   `EIO`, `ENOSPC`, `EMFILE` and `ENOENT` on the open all throw. The sentence describes the
   **superseded** 2026-08-06 rule; `fs-ops.ts:59-63` says so in as many words ("The open() leg
   propagates too (2026-08-11, superseding the 2026-08-06 ruling that swallowed every open
   failure)"), and `test/memory/fs-ops.test.ts:32-34` pins the new behaviour under the name *"a
   missing directory PROPAGATES on POSIX"*. So a shipped, green test contradicts the shipped
   document. This direction is *pessimistic* — the document confesses a silent-durability-loss path
   the code does not have — but a security document that misdescribes its own durability boundary is
   wrong either way, and a reader deciding whether to trust an acknowledged append is given the
   wrong rule.
2. **The suppressed class is six codes, not four.** `EPERM` and `EACCES` are in
   `DIR_FSYNC_UNSUPPORTED`, and `isUnsupported` is applied to **both** legs (`:73` and `:79`), so a
   directory fsync that fails `EPERM`/`EACCES` is silently swallowed. The next sentence —
   "Any other failure (`EIO`, `ENOSPC`, and their class) … now **propagates**" — is therefore false
   of those two codes. This direction is *optimistic*: it promises propagation where the code
   swallows, which is the dangerous way round. Note the source's own comment (`fs-ops.ts:36-38`)
   claims `EPERM`/`EACCES` "join them for the open() leg", which the fsync leg's shared
   `isUnsupported` call contradicts; that comment is wrong too, but it is a code comment and outside
   this audit's remit to change.

**Recommended replacement for `SECURITY.md:383-390`** (the surrounding sentence structure is
preserved; only the classification is corrected):

> **errno alone, never on the message**: if the fsync call, or the `open` that precedes it, fails
> with `EINVAL`/`EISDIR`/`ENOTSUP`/`EOPNOTSUPP`/`EPERM`/`EACCES`, the platform or the directory's
> standing permissions genuinely cannot fsync a directory (some filesystems reject it outright —
> `ENOTSUP`/`EOPNOTSUPP` are a second pair some filesystems return instead of `EINVAL`/`EISDIR`; the
> same numeric value on Linux but distinct symbols elsewhere) and the failure is suppressed. Windows
> cannot open a directory for reading at all and stays wholesale best-effort on the same path.

The following sentence ("Any other failure (`EIO`, `ENOSPC`, and their class) … propagates") then
becomes true as written, and should additionally name the open leg: a directory that cannot be
opened for an `EIO`/`ENOSPC`/`EMFILE`/`ENOENT` reason throws rather than reporting success.

### Correction 2 — imprecise: the audit trail's directory fsync is suppressed on every append, not on first creation

**Current text (`SECURITY.md:394-399`), verbatim:**

> The audit trail
> (`audit.jsonl`) is the one exception: it is documented best-effort/non-transactional already (see
> its own docstring), and its directory fsync on first creation stays unconditionally suppressed, so a
> disk hiccup on that side channel never reports an already-succeeded operation as failed — or, at a
> rejection site, replaces the real rejection error with an unrelated one on its way out.

`src/audit.ts:143-147` suppresses the directory fsync on **every** `appendAudit` call, and
`src/audit.ts:131-134` states the reason the "first creation" reading is no longer the shipped one:

> EVERY append fsyncs the directory, not just the first (2026-08-11): an exists-then-decide probe
> raced concurrent first creation, so a non-creator's acknowledged row depended on the CREATOR
> surviving to its own fsyncDir.

The guarantee's *direction* is unaffected — it is suppressed, which is what the sentence promises —
but the scope is understated, and a reader could conclude that a later append's directory-fsync
failure propagates. **Recommended replacement for the clause only:**

> …and its directory fsync — attempted on every append, not only on the one that creates the file —
> stays unconditionally suppressed, so a disk hiccup…

## What is not covered

This is the half of the audit that decides how much the other half is worth. Nothing here is
softened.

**1. The entire trust-resolution section has no documentation lock (claims 88-101, 14 claims).**
`test/docs/shipped-claims.doc.test.ts` does not mention the ceremony, `trust-pending`,
`resolveTrust`, `--repair` or `--fresh` anywhere. The two behaviour suites that exist
(`test/memory/trust-pending.test.ts`, `test/scripts/trust-resolve-cli.test.ts`, 17 cases, all green)
never open `SECURITY.md`. So every sentence in the newest and least-reviewed section of the document
can be reworded, weakened, or deleted with the suite fully green. This audit verified all fourteen by
execution today; that is a point-in-time fact about `0bcd4d7` and nothing more. The two claims most
worth a lock are #98 (`--fresh` does not delete) and #99 (the compaction chokepoint), because they are
the promises a user relies on *before* running an irreversible-sounding command, and #99 in
particular has no committed test that drives the store's real predicates after a rotation — the
existing `provesKey` tests all inject stubs. Recommendation for the next hardening pass, not for this
release: extend the doc-lock file with a case that drives a rotation and requires the sentence.

**2. Claims resting on reading alone (36 of 121).** These carry no mechanical guard and were not
executed: **7** (no allow-list `confirm`), **13** (state set / reverify-before-use), **25**
(citation residual), **28** (availability direction), **29** (cap vs migration trigger margin),
**37** (unforgeable-at-append-surface, as a quantified statement), **39** (position decides
ownership), **40**, **41**, **42** (the rollback-alarm gate and its narrowness — read at
`witness-write.ts:83-88` but never driven here), **45**, **46**, **47**, **48** (the MAC-v2 corollaries
about v1 records and `--asOf`), **51** (marker fixpoint), **53**, **56** (`ledgerOf` global fallback),
**57**, **58**, **59**, **60**, **62**, **63**, **65**, **67**, **68**, **69**, **70**, **72**,
**74**, **75**, **76**, **77**, **81**, **82**, **85**, **86**, **87**, **88**, **101**, **102**,
**105** (the fsync-temp-and-directory pair), **107**, **112**, **113**, **115**, **116**. Several of
these are residual statements whose truth is entailed by a claim that *was* executed (60 by 33+36; 57
by 33; 85 by 52); those are the safest of the set. The least-covered genuinely mechanical ones are
**40/41** (an elevated verify refused under a mismatch verdict — a security control with no doc lock,
verified here only by reading), **56** (the surviving global-ledger fallback), and **107** (the
at-least-once append contract).

**3. Claims with no test lock at all — even where this audit executed them.** Execution by an auditor
is a photograph; a doc lock is a tripwire. The following were driven today but nothing stops the code
or the prose moving apart tomorrow: **4**, **5**, **9**, **10**, **12** (the whole
adopt/recheck/confirm grade story), **14-18**, **22**, **23**, **24** (every write-path secret-scan
claim — the doc lock covers only the *egress* exemption sentence, #21), **19**, **20**, **27**
(override-proofness and per-leg policy), **30** (the DATA quarantine — the single most load-bearing
prompt-injection claim in the document, and `SECURITY.md` has no pin on it at all), **31** (no
telemetry — the source sweep is this audit's, not a committed test; `test/no-registry-exec.test.ts`
covers a different property), **34** (`HELIX_LEDGER` relocation), **38** (first-row-owns-id), **43**
(digest injectivity), **44** (MAC v2 tx binding), **71** (witness shape/domain), **78-79** (the
re-baseline ceremony's TTY gate and non-registration), **88-101** (all of §1 above), **106**
(hard-link refusal), **108-110** (the durability bullet — which is precisely where both defects were
found, and is not a coincidence), **121** (codex-log).

**4. Off-tree facts.** Claim **2** (that GitHub private vulnerability reporting is *enabled* on the
repository) cannot be checked from a working copy; it rests on the July sweep's end-to-end exercise
(`GHSA-m7p7-4mx7-jw96`, 2026-07-26). Claim **3** (acknowledgement within a few days) is a maintainer
undertaking with no mechanism. Claim **111** (`/clear` does not restart a session's MCP server) is a
dated live observation of Claude Code host behaviour with no in-repo implementing code; it is
honestly labelled as such in the document, and this audit re-derived nothing about it.

**5. Platform coverage.** Every execution in this record ran on Linux (`WSL2`, Node v24.20.0). The
macOS and Windows halves of claims **103** and **104** were exercised only through
`classifyHolder`'s injected identity seam — real behaviour on those platforms is unmeasured, here and
in the suite. Likewise, the `platform === 'win32'` branch of `fsyncDir` (claim **108**) was driven
only through the seam.

**6. A gap that is not a falsehood.** The at-rest section (`:431-444`) documents the creation mode of
exactly two files, `audit.jsonl` and `codex-log.jsonl`. Helix writes nine more under the home —
`memory.jsonl`, `sessions.jsonl`, `metrics.jsonl`, `trigger.jsonl`, `witness.json`,
`witness-log.jsonl`, `projects.json`, `ledger-mac-master.key`, and it hardens a user-supplied
`config.json` (`src/memory/home-permissions.ts:9-21`). All were observed at `600` in a temp home, and
claim **119** covers the startup repair for all of them, so nothing here is *wrong*. But
`metrics.jsonl` in particular is written **by default** (`DEFAULT_CONFIG.metrics.enabled === true`,
`src/config.ts:140`) and is disclosed only in `README.md`; a reader who consults `SECURITY.md` alone
to learn what Helix persists will not learn it exists. The July C3.3 record asserted metrics was
"disclosed in README/SECURITY" — of those two, only README is true today. Recommended as a one-line
addition to the at-rest section, not blocking.

**7. What this audit itself touched.** One error is on the record. An early probe called
`buildServer(store)` without setting `HELIX_HOME`; the server's audit path falls back to
`process.env.HELIX_HOME ?? join(homedir(), '.helix')` (`src/server/helix-server.ts:51,61`), so a
single `{"kind":"adopt","ts":"2026-09-09T06:10:27.262Z","scope":"/tmp/c3-frame2p-vEN7wb"}` row was
written to the real `~/.helix/audit.jsonl`, creating that directory. `~/.helix` had not existed and
contained nothing else; the row and the directory were removed and the prior state (absent) restored,
and every subsequent probe set `HELIX_HOME` explicitly. No tracked file other than this one was
modified, and no ledger, key or registry outside `/tmp` was read or written. Recorded here because an
audit that hides its own contamination is not an audit.

---

## Disposition of the corrections — applied 2026-09-09, before the candidate cut

Both corrections above were **applied** in the same change that commits this record, which was
possible only because the candidate had not yet been cut: after the cut, editing `SECURITY.md` moves
a claim-block id the receipt pins and retires the certification. The audit deliberately did not edit
the document while it was measuring it; the edits are a separate, later act.

- **Correction 1 (the FALSE claim)** — the durability bullet now says that the suppressed class is
  `EINVAL`/`EISDIR`/`ENOTSUP`/`EOPNOTSUPP`/`EPERM`/`EACCES` on **both** legs, that Windows cannot
  open a directory for reading and stays wholesale best-effort, and that any other failure on either
  leg propagates. The dangerous direction is closed: the document no longer promises propagation for
  `EPERM`/`EACCES`, which the code swallows.
- **Correction 2 (imprecision)** — the audit trail's directory fsync is now stated as attempted on
  every append rather than on first creation.
- **The source comment this audit noted but placed outside its remit** was corrected in the same
  change. `src/memory/fs-ops.ts` said `EPERM`/`EACCES` "join them for the open() leg"; `isUnsupported`
  is applied to both legs, so the comment described a narrower rule than the code has. It carries no
  shipped bytes — esbuild strips source comments — so the bundle is unmoved.

**One gap this audit named is now closed.** The trust-resolution section had no documentation lock,
so all fourteen of its claims could be reworded or deleted with the suite green.
`test/docs/shipped-claims.doc.test.ts` now recovers the ceremony's invocation by executing the
shipped CLI and requires `SECURITY.md` to name every mode it prints, and it pins the rotation
promise (`stay \`Fresh\` rather than being deleted`) together with the mechanism that makes it true,
recovered from `src/memory/ledger.ts` as `mayDrop = keyProven && singleLineage`. Mutation-verified on
the document side only — the promise was reworded in `SECURITY.md` and the case went red — which is
the direction that matters for a documentation lock, and is less than "both directions"; an earlier
version of this paragraph claimed the wider check. The remaining items under *What is not covered* stand as
written.
